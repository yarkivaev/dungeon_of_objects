as yet unclassified
setFrames: framesCollection frameDuration: time x: posX y: posY width: aWidth height: aHeight
	frames := framesCollection.
	frameDuration := time.
	position := posX @ posY.
	width := aWidth.
	height := aHeight.